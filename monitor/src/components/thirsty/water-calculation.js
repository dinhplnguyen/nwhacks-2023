import { StatusBar } from 'expo-status-bar';
import { StyleSheet, Text, View } from 'react-native';



addwater(int intakeW) {
  let water = ._____; 
  let newWater = 1.25 * (water + intake);
  if (newWater < weight){
  return newWater
  } else {
    weight
  }
}

addweight(int intakeF) {
  let food = ._____; 
  let newfood = (food + intakeF);
  return 66.5 + (9.663 * .Weight) + (1.850 * Height) - (4.676 * age)
}

export default function App() {
  return (
    <View style={styles.container}>
      <Text>Testing testing</Text>
      <StatusBar style="auto" />
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
    alignItems: 'center',
    justifyContent: 'center',
  },
});
